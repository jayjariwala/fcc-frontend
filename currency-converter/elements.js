export const app = document.querySelector('.app');
export const form = app.querySelector('form');
export const fromSelect = app.querySelector(`[name=from_currency]`);
export const toSelect = app.querySelector(`[name=to_currency]`);
export const amountInput = app.querySelector(`[name=from_amount]`);
export const result = app.querySelector(`.to_amount`);
